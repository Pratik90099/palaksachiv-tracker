# --- Network ---
PORT=8787
# Comma-separated list of frontend origins allowed to call this server.
ALLOWED_ORIGINS=https://your-frontend-domain.com,http://localhost:5173
# Public URL of your frontend app (used to build reset links).
APP_URL=https://your-frontend-domain.com

# --- Crypto (rotate this in production) ---
JWT_SECRET=b1bee64df8af30388b4d495b01f381182851bd7869d1fc3005e74404faefa41a962c38741bdb078fdd14dbde8fb1dc34

# --- SMTP (Gmail example; works with any SMTP provider) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=youraddress@gmail.com
# For Gmail: create at https://myaccount.google.com/apppasswords
SMTP_PASS=your-app-password
SMTP_FROM="GS Portal <noreply@yourdomain.com>"
